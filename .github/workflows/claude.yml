name: claude

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  pipeline:
    uses: MaciejMajchrzak/CI.Infra/.github/workflows/claude-service.yml@main
    permissions:
      contents: read
      packages: write
    with:
      project-name: ci-connector-openbanking
      api-project: src/CI.Connector.OpenBanking.API/CI.Connector.OpenBanking.API.csproj
    secrets: inherit
